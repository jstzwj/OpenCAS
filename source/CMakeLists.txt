FILE(GLOB_RECURSE HEADER_LIST ${CMAKE_SOURCE_DIR}/include/*.h*)
FILE(GLOB_RECURSE SOURCE_LIST ${CMAKE_SOURCE_DIR}/source/*.cpp*)

include_directories(${CMAKE_SOURCE_DIR}/include)

ADD_LIBRARY(opencas STATIC ${SOURCE_LIST})

TARGET_LINK_LIBRARIES (opencas PRIVATE ${CONAN_LIBS})


